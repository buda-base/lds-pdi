#QueryScope=Topic
#QueryReturnType=Table
#QueryResults=the list (Id, topic names) of all topics
#QueryParams=NONE
#QueryUrl=/lds-pdi/query?searchType=pdi_t_list

SELECT DISTINCT ?ID 
(GROUP_CONCAT(DISTINCT ?topic;   SEPARATOR=" || ") AS ?topic_Names)
where{  
	
    ?ID a :Topic . 
    ?ID skos:prefLabel ?topic .
}
GROUP by ?ID
order by ?ID