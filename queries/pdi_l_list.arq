#QueryScope=Lineage
#QueryReturnType=Table
#QueryResults=the list (Id, name) of all lineages
#QueryParams=NONE
#QueryUrl=/lds-pdi/query?searchType=pdi_l_list

SELECT DISTINCT  ?lineage ?lineage_name 

WHERE   { 
  ?lineage a :Lineage  .
  ?lineage skos:prefLabel ?lineage_name .
}